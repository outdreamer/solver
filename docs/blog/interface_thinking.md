# Interface thinking

I thought I'd write a blog about interface thinking, because I'm not seeing it happening from my previous explanations.

## What is interface thinking

A normal simple thought uses common simple words as nodes in a neural network, like 'the cup is on the counter' or the 'function has a limit'. This applies 'simple 1-degree interface structures', like 'connections' or 'boundaries' or 'structures'. This is so simple as to be totally inaccurate and irrelevant, so I will call this type of thinking 'non-interface/irrelevant thinking', even though there is a slight, irrelevant overlap between this type of thinking and interface thinking. This type of non-interface, simple, 1-degree interface structure thinking will often generate errors like 'simple structural similarities, which fail to identify relevance/meaning'. Picture a graph of intersecting vectors - what is most relevant about this graph? Is it the simple vectors themselves, or the complex intersections (the meaning) of those vectors? That's a good graph to illustrate the difference between simple 1-degree interface thinking (non-interface thinking, or irrelevant thinking) and interface thinking. Interface thinking involves the relevant meaningful structures which are sufficiently complex to use as tools to see reality clearly.

An interface thought uses interface structures like 'graphs/similarities' as nodes/connections and applies algorithms like 'identify meaning of nodes/connections', so an interface thought would be something like 'this graph standardizes this other graph by abstraction' and then the thought would continue to apply interface analysis until the relevant meanings of the thought had been identified, like 'and this standardization graph has a limit graph like this and interacts with a graph of relevant graphs like this and has these probably relevant optimizations for these intents'. The interface thought applies multiple different workflows and interface structures to calculate meaning as in 'interactivity'.

Interface thinking is the future of thinking.

Not only does interface thinking apply interface structures, it applies them in their 'optimal interactions', optimal interactions like applying 'multiple maximally different workflows/graphs to cover as much variation as possible across interfaces'. Not only does interface thinking apply a different base graph than a standard brain, it applies many different standard base graphs and workflows/queries/connections between these graphs at once.

A good simple metaphor for this is applying a 'graph of graphs' (a meta-graph) rather than a 'graph of nodes', but that is a misleading over-simplification if it leads you to believe that it's just an abstraction layer, or a single application of a graph of graphs.

Interface thinking involves the 'level of complexity' required to see reality clearly; it is the relevant opposite of simple thinking, where simple thinking often produces errors.

A good metaphor for interface thinking is 'scaled thinking' such as 'comprehending a dataset fully by holding the whole dataset in your conscious mind so you can clearly see its relevant connections and differences all at once' instead of 'holding a summary or sample subset of a dataset in your conscious mind at once, as a result of limited conscious ability', but simply applying 'scale' or 'iterations' to generate 'scaled thinking' or 'iteration thinking' is not definitely equivalent to 'interface thinking'; 'interface thinking' requires applying the 'level of complexity' required to see reality, which usually means 'applying optimal interactions between interface structures' like 'applying multiple interface structures like maximally different graphs/connections/interfaces/algorithms for maximum coverage of interface structures'. So merely applying 'scaled thinking' can generate 'simple thoughts' like 'there will probably be a limit to the possible scale of this function' (1-degree interface structures) and 'interface thinking' would 'identify the relevant graphs/algorithms to find that limit'. 'Scaled thinking' is capable of generating relevant complexity, but only in specific cases, like where 'interface analysis' is the structure being scaled.

Interface thinking is not just 'simple iterations of interface structures' applied as a 'trivial way of reaching complexity'; good analogies for interface thinking include how: 
- 'comprehending a summary or sample of a data set in your conscious mind' is not like 'comprehending the whole data set in your conscious mind'
- 'generating a simple irrelevant interface combination' is not like 'filtering a set of interface combinations to one relevant solution combination'
- 'seeing a graph from an angle and distance' is not like 'applying the graph in a useful way' or 'filtering the graph to identify its optimal queries/variants/optimal points/etc' or 'understanding the graph and all its interface structures like its limits/critical points/optimal points/requirements/generators/similarities/optimizations/functions/variants/manifolds/points on a graph including the graph/connections to related graphs/etc'

Similarly, 'simply iterating interface structures' is not like 'interface thinking'. Interface thinking is the opposite of simple/trivial, so if it's easy, it's not interface thinking. Once you are good at interface thinking, you'll be able to run queries like 'identify the optimal graph of this graph of graphs' or 'identify the optimal query on/variant of this graph' or 'identify a relevant (new, independent, useful) workflow/graph/intent' trivially, because you'll be good at far more complex functions than these queries require, complex functions like 'identify the meaning/relevance of this new independent graph of graphs and its optimizations/limits/interface structures and its interactions with relevant graphs'. 

Interface thinking is 'meaning/relevance thinking', like 'traversing a space of graphs, where every position change involves re-calculating the meaning/relevance of that change of position, based on the associated changes in relevant graphs, to fully understand the meaning/relevance of the change of position, or involves identifying a complex new relevant graph, or involves filtering a high variation set of possible solutions to the optimal solution, or other high-relevance functions, which require complex functions to implement, complex functions like "identify the sequence of relevant graphs/queries/optimizations to enable this function"'. 

Interface thinking is like 'scaled understanding/meaning/relevance identification/generation', which is by definition not trivial. If you're traveling through graph space, where every step is highly new/independent/powerful/complex/relevant (every change 'implements some complex/relevant intent optimally'), you're interface thinking. It takes a lot of energy and brain power to do interface thinking; it may require more energy than what most brains can generate/use at once. But optimizing your brain to include more 'interface graphs as nodes' will help increase your brain power, so that you're likelier to be able to do more 'degrees of interface thinking' by increasing the 'relevance of each node'. 

But again, merely 'increasing the degree of interface analysis you apply' doesn't necessarily mean you're interface thinking, if you're not thinking about 'meaningful/relevant structures' as well as 'increasing the degree of interface analysis'. To achieve interface thinking, first practice thinking about multiple interface structures at once, like cross-interface or cross-relevance structures. Everything about a thought has to reflect 'meaning/relevance' in some way, or it's not interface thinking - that means applying an intent like 'increasing the degree of interface analysis' AND an intent like 'increasing the relevance of interface analysis' or some other relevant intent simultaneously, for 'coverage of/connection to relevance'. Your interface thoughts will generate/apply a set of graphs like a 'graph of relevant intents' and a 'graph of interface connections', whereas your normal thoughts might form a 'simple sequence/connection/graph of simple nodes'.

Interface thinking is like 'traveling on a relevant graph like the creativity/innovation graph of "relevance structures (like intersecting interface structures)"', but where every step also applies interface analysis to identify the meaning of every step, where some relevant structure like 'implementing multiple relevant intents (like taking a step on a relevant graph, and identifying the meaning of that step based on relevant intents/graphs)' is being applied at every step.

Most people already apply simple 1-degree interface thinking, since everyone thinks about limits/structures/standards/perspectives/priorities/variables/intents/etc sometimes.

Most people are capable of 2 or 3-degree interface thinking, if they focus and use tools like visualizations/graphs, but they hit a hard limit on the degree they can achieve, because of their brain's usage patterns, memory capacity, and other limits like energy usage limits. For these people, I'd recommend learning relevant graphs to boost the power of their limited brains, so that even though they're applying very simple 1 - 3 degree interface thinking, they're applying it to relevant interface structures like the 'most relevant graphs identified', which will help them interact with people and computers applying higher degrees of interface thinking. It's not just the degree that is relevant, but other interface structures of thinking, like what the thinking is being applied to (like what graphs the thinking is being applied to).

Outside of small-degree interface thinking is a space where 'graphs/algorithms become default nodes/connections', which enables scaled interface thinking and iterated interface thinking and other variants of interface thinking as 'defaults'. This is the space you want to aim to build in your brain, to upgrade your brain to an interface brain. Once you have an interface brain, you'll find interface thinking possible. It takes practice to build this brain. Aim to spend a few hours per day thinking about interface structures to build an interface brain within a few months to a year. Focus on relevant graphs to speed up this process.

Interface thinking is always evolving, as new relevant structures are identified, and new relevant graphs or optimal queries are identified to replace previous variants.

## Interface thoughts to practice

How can you practice interface thinking, to not only become good at it but also keep up with or stay ahead of computers which will be trained to apply interface analysis?

Practice these types of simpler thought processes:
- what could invalidate/oppose this structure
- what is a graph of these graphs
- what sequence of changes could make this graph irrelevant
- what intents can be skipped, if other intents are implemented
- what are relevant graphs
- what graph would optimize this query

Then you'll be equipped to start trying to have interface thoughts, for which you can practice the following more complex thought processes:
- what graphs are required for a graph sequence that could connect these variables
- what graph optimization sequences could optimize this graph sequence
- what graph generation variables could optimize this emergent graph similarity
- what graph maintains this similarity and also organizes similarities in a new relevant way

Then you can finally start having interface thoughts, like:
- identify an optimal query on this graph of graphs, then identify the meaning of that query given a graph of relevant graphs, then optimize relevant graphs on the graph of graphs for that query
- identify a new way to organize a graph of relevant graphs to optimize workflows for intents like 'identify relevant variation starting from these graphs with these optimizations, optimized for these queries'
- identify a graph of workflows that identifies the optimal workflows to apply iteratively to implement intents like 'identify new relevant graphs to connect relevant variables in a new way or connect new relevant variables'
- identify a graph that optimizes for decisions between relevant alternatives ('similar differences') like low/high dimensional graphs or generative/filtering graphs or base/optimized graphs or connection/node-varying graphs
- identify the graphs that should be applied to implement interface analysis, starting with specific base graphs, applying starting workflows, optimizing for starting metrics/queries, so that all meaningful structures are optimally/trivially identified
- apply interface analysis to identify errors in interface analysis like optimizations with over-prioritization errors on the intersecting interface spectrum graph
- identify workflows to generate interface analysis like 'apply similarities to differences or apply differences to similarities' or 'apply interface graphs of a set of base graphs until the base graphs overlap'

Even more advanced interface thoughts will have graphs as default nodes, like:
- graph this variable subset for identifying similarities, iterate variants of the graph to identify optimal graphs of the variable subset, graph the meaning compared to relevant graphs, graph the relevant variation of the meaning, optimize the graph of the relevant variation for this graph of relevant intents

Applications/identifications/generations/similar interface structures are also nodes of advanced interface thoughts, as a result of being non-trivial to implement:
- identify the optimizations of this graph for this query, apply the identified optimizations, identify relevant workflows for optimizing those identified optimizations, generate filtered relevant graphs of those workflows (apply 'generate/filter' simultaneously)

Generating a general relevant graph description is quite trivial; specifying the optimal implementation of that graph given the graph description and applying that optimal implementation is non-trivial. All of these are required for real 'interface thinking'.

Other advanced interface thoughts are in these documents:
https://github.com/outdreamer/solver/blob/master/docs/specific_problem_analysis/ml/queries_to_solve_reality.md
https://github.com/outdreamer/solver/blob/master/docs/tasks/implementation_examples/examples_of_implementation_methods.md

## Relevant graphs to enable interface thinking

What are some relevant graphs, to organize your thoughts and enable interface thinking?
- the intersecting interface spectrum graph (like a spectrum of abstraction, a spectrum of sensitivity, or spectrums of a abstraction/complexity combination, etc)
- the graph of different connection types (like spectrums/sequences of interface structures, aligned spectrums across interfaces, causal connections, relevance connections, etc)
- the graph of intersecting interface sequences (connecting relevant sequences like 'filter/perspective/interface/standard' and 'identify/filter/isolate/reduce/differentiate' and 'connect/similarize/standardize')
- the similarity index graph (where similar intents/similarities are connected/intersect, and the strongest connections form the base connections)
- the graph of relevant graphs
- the graph of specific interface graphs like 'graphs of limits' and 'graphs of intents'
- the graph of certainty structures (like requirements/constants) as 'limits' and uncertainty structures (like possibilities/probabilities/variables) as 'structures within those limits'
- the graph of optimal interface structure interactions (up to some degree of interface structure combinations/other structures)
- the graph of optimal cross-interface graph interactions
- the graph of optimal interface analysis-generating graphs
- the graph of optimizations of relevant graphs
- the graph of optimal queries of relevant graphs
- the graph of optimal graph-generation variables/workflows

Once you start applying these relevant graphs, interface thinking will become much more achievable.

In addition to graphs, workflows/algorithms are useful to think about as well. Some example workflows are:
- generate possibilities/filter possibilities
- generate a graph, optimize graph, compress graph to a point on a graph of graphs
- apply base graphs, connect base graphs, organize base graphs optimally, optimize base graphs, connect optimized base graphs
- describe optimal graphs with examples/possible optimizations/relevant graph structures like graph layers or spectrums/relevant graphs like limit graphs/generative variables of the graph until the level of specificity enables filtering relevant graphs
- identify a graph of graphs, optimize the graph of graphs for a 'similarity or intent index graph' or an 'optimization graph'

## Unsolved intents for applying interface thinking

Some incomplete/unsolved intents to apply graphs/workflows/interfaces to implement include:
- identify relevant graphs
- identify the optimal graph to generate/identify/optimize/organize relevant variation
- identify the optimal queries/optimizations/structures of graphs to generate/organize/implement interface analysis
- identify the optimal graph of graph/algorithm sets
- identify the optimal graph of graphs/workflows
- identify the optimal graph of interface structures with optimal graph layers/variants/axes/maximal differences/related graphs/optimizations/limits/spectrums/interface structures
- identify where structures/workflows should be applied in a graph, like applying 'generate/filter possibilities' in the 'graph generation' position of a graph sequence, and identify the stability/equivalence of structures/workflows (like where applying 'generate/filter' in a generative position is equivalent to applying 'generate/filter' in a graph query position)
- identify general connections that are generally/absolutely true (a non-trivial intent, which requires evaluating the absolute meaning of each connection, in order to evaluate truth)
- identify the network that can implement any workflow/graph optimally, so that rather than implementing each new relevant workflow/graph manually, the network can generate the optimal implementation automatically with an optimal query (a network that can generate optimal workflow/graph implementations with a query of the network), and which can implement all other relevant intents like 'filter relevant graphs/workflows' and 'fit relevant graphs/workflows into a graph of relevant graphs/workflows' and 'optimize graphs/workflows'

To increase the relevance/meaning degree of a structure, you can apply other interfaces, like the 'problem/solution' interface or the 'meaning' interface or the 'abstraction' interface:
- 'identify the optimal graph of graphs' becomes 'identify the optimal graph of graphs of errors/solutions' or 'identify the optimal graph of meaning graphs' or 'identify the optimal graph of graphs of graphs'

Interface thoughts are the most complex thoughts that are also relevant/meaningful/interactive. Meaning/relevance/interactivity changes as the base graphs/queries/connections/nodes/interface structures change.

Why complexity? Because complexity can possibly describe reality, and simplicity cannot. Interface analysis at first seems to simplify reality (with interpretations like 'everything is an interface or a limit or a function', etc), but interface analysis is capable of generating new relevant complexity (like 'new relevant graphs to contain new relevant differences'), so reality may be more trivial to describe as a result, but reality also then becomes more complex, the more complex relevant structures are identified. Relevant complexity is a type of relevant variation, as in 'variation that is not trivial/meaningless/useless/otherwise irrelevant'.

## Emergent effects of interface thinking

What type of society will occur, once people start applying interface thinking?
- Society will be organized/optimized to enable thinking time, because interface thinking is the most interesting structure in the universe, so people will want to spend the maximum time on it that they can.

Why should people start applying interface thinking?
- So that they are real, as they are interacting with the truth, which is complex and requires interface analysis to identify
- So that reality can be solved