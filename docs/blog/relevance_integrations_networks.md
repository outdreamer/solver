# Integrations of relevance and neural networks

Integrating relevance in neural networks increases their predictive ability, similar to how training neural networks on 'interface data' like 'interface definitions' increases their predictive ability because interface structures are highly predictive, and similar to how integrating relevant graphs with neural networks increases their predictive ability by applying 'identified connections of interface variables' from the relevant graphs. 


## Examples of integrations of relevance and neural networks


### Components

Cross-relevance structure:
- cross-relevance structures like 'intents/functions' or 'functions/structures' as component vectors of relevance, multiplied to generate relevance

Relevant interface interaction:
- applying interface variables as different 'parameters of polynomial functions' with varying coefficients indicating the different interactions of interface variables


### Vector sequences

Relevance implementation:
- relevant (like maximally different) vector sequences, as relevance implementation structures

Relevance generation:
- different relevance structures of polynomials like 'different ways to generate polynomials' (like different component types like 'subsets/parameters of polynomials' and 'polynomial types like waves') as different sequences in the network

Relevance definition implementation:
- applying different vector sequences from relevant graphs like the 'relevance definition network', vector sequences that define relevance (like vectors that define uniqueness/importance/generality), as a way to generate new relevance


## Activation thresholds

Relevance definition implementation:
- applying relevance definition thresholds like a 'lower limit on generality sufficient for relevance' as activation thresholds in a network to implement the definition of relevance in 'what changes are allowed in a network'


## Change propagation functions

Relevance definition implementation:
- applying different similarities like 'adjacence/cause/requirement' of the definition of relevance as filters in the network like as filters of 'inputs to change propagation functions between network layers'


### Network/variable subset connections

Relevant problem-solving:
- different subsets of the network applied to identify polynomial functions connecting different relevant variable sets like 'maximally different variable sets' as 'problems to solve in the network', where different subsets generating polynomials are organized in the network by what polynomials are useful to generate other polynomials
- applying 'relevant functions, which are relevant to connect' as different polynomials, applied as default nodes in a neural network and positioned to optimize for relevance of 'adjacent functions' and 'function sequences'