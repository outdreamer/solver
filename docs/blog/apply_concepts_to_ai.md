# Applying concepts to AI

Abstract concepts like randomness, truth, balance, power, volatility, sensitivity, etc are useful to generate new inventions.

Specifically how can they be used to generate inventions?

Assuming the intent is to 'connect concepts with solution structures (neural network or graph structures)', various methods to implement this intent apply interface analysis.


## Applying alternate definitions of concepts

Applying different definitions of concepts like 'randomness' can be useful to identify 'different structures of the concept', and identify 'different interactions' of these 'structures of the concept' with 'structures of the solution' like 'neural network/graph structures'.

Example: 'randomness' can be defined as 'luck distribution' or 'distant causality' or 'lack/minimum of understanding/information' or 'equivalence between alternates', and applying these definitions to neural networks/graphs can be structured as 'graphs with indirect/distant causal connections' or 'graphs with minimal local information which builds maximal global information'.


## Applying a system of optimal concept interactions

Applying the 'system of optimal interactions of interface variables (like randomness)' to identify the 'optimal interactions between concepts' (like 'how randomness interacts with relevance/optimality') can identify 'optimal structures to implement by connecting to solution/neural network/graph structures'.

Example: it's "optimal to apply 'randomness' as a 'filter' when 'there is minimal information' or 'alternatives are equivalent'", so 'randomness' can be applied in 'filter functions of neural networks/graphs, when either case applies'.


## Applying the concept to specific solution structures

Applying the concept to specific solution structures like 'neural network outputs/functions/workflows' or 'optimization metrics/generative variables of a graph', where the concept is applied as an 'input/output/other simple structure of the specific solution structure', is a trivial way to identify possible connections between the concept and solution structures.

Example: 'randomness' is an output of the neural network, 'randomness' is a generative variable of a graph.


## Applying relevant graphs of the concept

Applying the relevant graphs of the concept like the 'graph of limits/requirements/variants of randomness, to identify the relevance of randomness' can be used to identify interactions between 'relevant graphs of the concept' and 'solution/neural network/graph structures'.

Example: applying the 'graph of limits/requirements/variants/relevance of randomness' to identify whether randomness can be relevant/useful when applied to a neural network structure, like applied as an 'input to a neural network function', like 'randomly selecting a weight from a local set of weights and discarding the other weights', which would have results like 'randomness is useful to select local weights, when the impact of weights is similar'.